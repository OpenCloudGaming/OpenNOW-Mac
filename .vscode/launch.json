{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug AppKit",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/OpenNOW",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build"
        }
    ]
}
